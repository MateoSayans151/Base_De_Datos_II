package service;

import entity.Sensor;
import org.springframework.stereotype.Service;
import repository.mongo.SensorRepository;

import java.util.List;
import java.util.Optional;

@Service
public class RegistroSensorService {
/*
    private final RegistroSensorRepository repo;

    public RegistroSensorService(RegistroSensorRepository repo) {
        this.repo = repo;
    }

    // LISTAR TODOS LOS REGISTROS
    public List<RegistroSensor> getAll() {
        return repo.findAll();
    }

    // OBTENER REGISTRO POR ID
    public Optional<RegistroSensor> getById(int id) {
        return repo.findById(id);
    }

    // CREAR NUEVO REGISTRO
    public void create(RegistroSensor registro) {
        repo.save(registro);
    }

    // ACTUALIZAR REGISTRO EXISTENTE
    public RegistroSensor update(int id, RegistroSensor registro) {
        registro.setId(id);
        return repo.save(registro);
    }

    // ELIMINAR REGISTRO
    public void delete(int id) {
        repo.deleteById(id);
    }

 */
}
